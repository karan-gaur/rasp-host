<form class="form-signin" method="POST">
    {{#each errorMessages}}
        {{selectedFile: selectedFile + this}}
        <button type="submit" class="btn btn-lg btn-primary btn-block">{{this}}</button>
    {{/each}}
</form>